{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window capability for the AgentOS Console main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
